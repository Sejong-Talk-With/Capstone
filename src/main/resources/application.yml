spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/capstone # jdbc:mysql://${DB_URL}:3306/semogong #
    username: park # ${DB_NAME} #
    password:  # ${DB_PASSWORD}
    driver-class-name: org.h2.Driver # com.mysql.cj.jdbc.Driver #

  jpa:
    hibernate:
      ddl-auto : update # update !!!! 꼭 AWS 쓸 때는 update 로 !!!!
    properties:
      hibernate:
        format_sql : true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace # p6spy로 대체
